import styles from "./footerstyles.module.scss"



const Footer = (props) => {
    return (
        <footer className={styles.footer}>
            Footer
        </footer>
    )
};

export default Footer;